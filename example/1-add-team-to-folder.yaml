name: Get Jenkins folder
http GET:
  url: ${xlrUrl}
  path: /api/v1/folders/find?byPath=JenkinsWorld
set:
  folderId: ${result.id}
print: ${folderId}
---
name: Get teams
http GET:
  url: ${xlrUrl}
  path: /api/v1/folders/Applications/${folderId}/teams
set: teams
---
name: Print teams
join:
- updatedTeams:
  - ${teams}
  - teamName: My New Team
    id: ${folderId}/NewTeam
    members:
    - name: admin
      type: PRINCIPAL
    permissions:
    - group#edit
    - folder#edit_configuration
    - folder#view
    - folder#edit_security
    - folder#edit
    - group#view
# http POST:
#   url: ${xlrUrl}
#   path: /api/v1/folders/${folderId}/teams
#   body: ${updatedTeams}
print_json: ${updatedTeams}

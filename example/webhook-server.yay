On Http request:
  /hello:
    GET:
      Print: Got request
      Output: Hello from Server!

  /hello/{name}:
    GET:
      Output: Hello ${name}!

  /echo:
    PUT:
      Output:
        headers: ${headers}
        body: ${body}

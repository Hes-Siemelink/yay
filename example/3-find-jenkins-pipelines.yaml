http GET:
  url: ${jenkinsUrl}
  path: /api/json
set:
  jobs: ${result.jobs}
---
foreach:
  job: ${jobs}
  do:
    print:
    - ${job.name} (${job._class})
    - ${job.url}
    - "   "
---
write file:
  name: jenkins-jobs.yaml
  content: ${jobs}

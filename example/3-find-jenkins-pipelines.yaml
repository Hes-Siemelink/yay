request:
  path: /api/json
  result:
    name: jobs
    path: $[jobs]
---
foreach:
  var: job
  in: ${jobs}
  do:
  - variables:
    - var: url
      value: ${job}
      path: url

  - print: ${job.name} (${job._class})
  - print: ${url}
  - print: "   "
